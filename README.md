# Vue PDF Viewer

Minimal Vue 3 + Vite example to select and view a PDF file.

Quick start:

```bash
cd vue-pdf-app
npm install
npm run dev
```

Open the local dev server, choose a PDF from the left panel — the right side will render the first page and allow prev/next navigation.
